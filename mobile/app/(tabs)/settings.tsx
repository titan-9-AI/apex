// app/(tabs)/settings.tsx — แท็บตั้งค่า (ตั้งค่าได้จริงในแอป เฉพาะแอดมิน)
import SettingsScreen from '../../components/SettingsScreen';

export default function SettingsTab() {
  return <SettingsScreen inTab />;
}
